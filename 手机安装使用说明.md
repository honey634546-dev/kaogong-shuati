# 手机安装使用说明（刷题 App）

> 版本：v1.0（2026-08-11 构建）· 平台：Android
> App 为**个人自用版**：题库、错题本、收藏全部在手机本地，不需要服务器、不需要登录、不需要网络（仅 AI 解析和未缓存的图形题需要联网）。

---

## 1. APK 文件位置

| 类型 | 路径 | 说明 |
|---|---|---|
| Debug 版 | `app\android\app\build\outputs\apk\debug\app-debug.apk`（43.7MB） | 联调用，可安装 |
| Release 签名版 | `app\android\app\build\outputs\apk\release\app-release.apk`（构建后生成） | **日常使用装这个** |

把 APK 传到手机的方式：微信/QQ 文件传输助手、数据线拷贝、`adb install`（开发者模式）均可。

## 2. 安装

1. 把 `app-release.apk` 传到手机，点击安装
2. 首次安装提示"未知来源应用"：允许即可（个人自用 APK，未上架应用商店）
3. 桌面出现「刷题」图标即安装成功

## 3. 首次启动

- 首次打开需要 **加载内置题库（36MB 压缩包）并解压建索引，约 5-20 秒**（视手机性能），期间显示加载进度，请耐心等待
- 加载完成后进入首页，**无需注册/登录，打开即用**
- 之后每次启动秒开（数据已缓存）

## 4. 功能说明

| 功能 | 说明 |
|---|---|
| 刷题 | 随机练习 / 按章节 / 按试卷 / 材料题组，行测职测 15 题、申论综应 2 题 |
| 判分 | 客观题自动判分 + 解析；申论/综应主观题可看答案，AI 批改需联网 |
| 错题本 / 收藏 | 自动记录，存在手机本地，杀进程/重启不丢 |
| 统计 | 刷题量、正确率、错题分布，按日/科目汇总 |
| AI 解析 | 设置页填入自己的 API key（OpenAI 兼容接口，如 DeepSeek），联网可用；断网时给出提示 |
| 智能组卷 | 行测/职测可生成模拟卷（模块配比参照国考），组卷后直接做题，带倒计时与自动交卷 |

## 5. 图片说明

- **公式图**：已随 App 打包（约 1900 张），离线正常显示
- **图形题**（约 5000 张真图形）：首次联网查看时自动缓存，之后断网也能看；未缓存时显示"图片需联网查看"占位

## 6. 常见问题

| 问题 | 处理 |
|---|---|
| 首次加载很慢 | 正常，36MB 题库解压建索引需数秒；之后秒开 |
| 卸载重装 | **错题本/收藏会清空**（存在应用私有存储）；题库会重新加载 |
| 更新 App | 直接装新 APK 覆盖安装，**错题本/收藏保留** |
| AI 不好用 | 检查 key 是否正确、是否联网；key 只存在本机 |
| 想换回服务器版 | 电脑上 `node server.mjs 3000`，浏览器打开 `http://localhost:3000`（功能与 App 一致，含多人登录） |

## 7. 版权与使用范围

- 题库数据来自粉笔（tiku.fenbi.com），**仅限个人自用，请勿公开发布、传播或商用**
- App 未上架任何应用商店，签名 keystore 仅本机持有

---

*构建命令（需 JDK 17 + Android SDK）：`cd app\android && gradlew.bat assembleRelease`*
