# 2026-09-23 公开真题库填空线修复

## 原因与范围

河南 2026 行测网友回忆版第 1 题，来源身份为 `gkzhenti:1775360735848:1`。
原始 HTML 用 `<u>` 包裹不换行空格表示填空线。导入器先去掉标签，再折叠空格，
导致已存储的材料只有普通空格。此前富文本渲染器对 `____` 的保护无法补回这些信息。

`htmlToText` 现先把空白的 `<u>` 转为 `____`，再执行原有文本清理。
覆盖空标签、普通空格、不换行空格、全角空格、HTML 实体、内层文字包装以及嵌套 `<u>`。
已有下划线、普通空格、画线文字和图片不被误判为空白填空。

## 已有数据恢复

`scripts/repair-gkzhenti-blanks.mjs` 从原始 HTML 派生修复前后文本，按 `external_id`
匹配当前版本。只替换仍与旧导入结果相同的题干、材料或选项，并更新内容指纹。
已被编辑的相关字段跳过；不改题目 ID、逻辑身份、版本、答案、解析、图片、批次或权限。
应用前备份受影响的完整行，在事务内写入；默认运行只预览，再次运行不重复修改。

```sh
node scripts/repair-gkzhenti-blanks.mjs --db=/path/to/practice.db --cache=/path/to/gkzhenti
node scripts/repair-gkzhenti-blanks.mjs --db=/path/to/practice.db --cache=/path/to/gkzhenti --apply
```

线上没有原始 HTML 缓存，本次使用本地缓存生成、校验后传入的同一修复映射。

- 线上：10 个批次、144 道题恢复，跳过 0 道；复查剩余待修复数为 0。
- 河南卷：16 道题，共 31 处填空线，包括第 1 题和第 7 题。
- 核对备份与修复后数据库，144 道题除允许修复的字段及指纹外全部一致。
- 线上导入器已更新；服务持续运行，本次修复无需重启服务。
- 线上行备份：`/srv/kaogong-shuati/data/blank-repair-backups/gkzhenti-2026-09-23T15-17-53-812Z-833841.json`。
- 本地规范化缓存：73 份试卷、1018 道题恢复，原文件保存在 `data/gkzhenti/blank-fix-backup-20260923/`。

## 验证

新增回归用例先复现失败，修复后以下 62 项测试全部通过：

```sh
AUTH_DISABLED=1 node --test test-gkzhenti.mjs test-rich-text.mjs test-custom-parser.mjs test-wp2-custom-bank.mjs
```

用例包括河南宋史题从原始 HTML 到富文本输出、多个空位、题干/选项/共享材料/文本回退、
修复预览无写入、备份、指纹、幂等、跳过人工修改及历史版本、保留题目身份和学习记录。

独立测试服务实际导入河南旧数据后执行修复，通过 Chrome 确认第 1 题横线可见，
材料全屏视图同样保留；第 3 题的两处填空线独立显示。

线上在新标签页重新进入河南卷，实际刷题页面的宋史题已显示引号内的横线；
题目列表中的第 6 题两处横线、第 16 题单处横线也正常。
验证未选择答案或交卷，用户原先打开的练习标签页未刷新。
